# TODO

- [ ] Đọc và xác định luồng UI trên `components/story-map.tsx` để thêm nút đóng/mở khi bấm.
- [ ] Cập nhật `components/story-map.tsx`: thêm nút toggle “Đóng/Mở” trong modal và đảm bảo bấm nút sẽ đóng/mở lại đúng mốc cũ.
- [ ] Kiểm tra compile (typecheck/lint) và chạy dev/build để xác nhận không lỗi.

